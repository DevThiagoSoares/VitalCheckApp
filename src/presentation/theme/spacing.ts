/**
 * Sistema de espaçamento consistente baseado em escala de 4px.
 *
 * Usar valores predefinidos ao invés de números arbitrários
 * garante consistência visual e ritmo no layout.
 */
export const Spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;

/**
 * Border radius padrão.
 */
export const BorderRadius = {
  sm: 8,
  md: 12,
  lg: 16,
  full: 9999,
} as const;

